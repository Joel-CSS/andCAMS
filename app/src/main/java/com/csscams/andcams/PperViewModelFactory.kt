package com.csscams.andcams

/*
class PperViewModelFactory(private val repository: CamsRepository): ViewModelProvider.Factory {

    override fun <T : ViewModel?> create(modelClass: Class<T>): T {
        if(modelClass.isAssignableFrom(PperViewModel::class.java)){
            @Suppress("UNCHECKED_CAST")
            return PperViewModel(repository) as T
        }
        throw IllegalArgumentException("Unknown View Model class")
    }
}
*/
