package com.csscams.andcams.database

/*TODO:
@Dao
interface CamsDAO{
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertPayPeriod(payPeriod: PayPeriod)

    @Insert
    suspend fun insertPayPeriods(payPeriods: List<PayPeriod>)

    @Update
    suspend fun updatePayPeriod(payPeriod: PayPeriod)

    @Delete
    suspend fun deletePayPeriod(payPeriod: PayPeriod)

    @Query("DELETE FROM PayPeriod")
    suspend fun deleteAllPayPeriods()

    @Query("SELECT * FROM PayPeriod")
    fun getAllPayPeriods()
}

*/